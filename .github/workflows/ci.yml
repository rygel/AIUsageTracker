name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      run_tests:
        description: 'Run tests'
        required: false
        type: boolean
        default: true

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
        rust: [stable]

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: ${{ matrix.rust }}
        override: true
        components: rustfmt, clippy

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: ~/.cargo/registry
        key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

    - name: Cache cargo index
      uses: actions/cache@v4
      with:
        path: ~/.cargo/git
        key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libsoup2.4-dev libjavascriptcoregtk-4.1-dev libwebkit2gtk-4.1-dev dmidecode

    - name: Check formatting
      run: cargo fmt -- --check
      working-directory: ./rust

    - name: Run clippy
      run: cargo clippy
      working-directory: ./rust

    - name: Build
      run: cargo build --verbose
      working-directory: ./rust

    - name: Run tests
      run: cargo test --verbose
      working-directory: ./rust
      continue-on-error: true

  build-cli:
    name: Build CLI
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc

    steps:
    - uses: actions/checkout@v4

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        target: ${{ matrix.target }}
        override: true

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libgtk-3-dev libsoup2.4-dev libjavascriptcoregtk-4.1-dev libwebkit2gtk-4.1-dev dmidecode

    - name: Build CLI
      run: cargo build --release --bin opencode-tracker --target ${{ matrix.target }}
      working-directory: ./rust

#    - name: Upload CLI artifact (Unix)
#      if: matrix.os != 'windows-latest'
#      uses: actions/upload-artifact@v4
#      with:
#        name: opencode-tracker-${{ matrix.target }}
#        path: rust/target/${{ matrix.target }}/release/opencode-tracker

#    - name: Upload CLI artifact (Windows)
#      if: matrix.os == 'windows-latest'
#      uses: actions/upload-artifact@v4
#      with:
#        name: opencode-tracker-${{ matrix.target }}
#        path: rust/target/${{ matrix.target }}/release/opencode-tracker.exe
