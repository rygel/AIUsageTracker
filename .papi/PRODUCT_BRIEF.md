# AI Usage Tracker

.NET 10 WPF Slim UI + Monitor (loopback HTTP, SQLite) + Web + CLI (`act`). Provider implementations in `AIUsageTracker.Infrastructure/Providers`. Version: `Directory.Build.props` `<TrackerVersion>`. Agent rules: `AGENTS.md`.
