# Mechanical Cleanup

This file previously contained an obsolete March 2026 warning snapshot. Use the current [analyzer cleanup work packages](analyzer-cleanup/README.md) instead.

The current mechanical scope is intentionally separated from behavior-sensitive analyzer work. Do not use broad formatting over the solution; assign one bounded package and validate only its declared files before running the full build.
