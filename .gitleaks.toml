[extend]
useDefault = true

[allowlist]
description = "Ignore generated scan artifacts accidentally committed in PR work"
paths = [
    '''^\.gitleaks-bin/''',
    '''^gitleaks-report\.json$''',
    '''^gitleaks_.*\.zip$''',
    '''^sonarqube_info_issues\.json$''',
    '''^sonarqube_issues\.json$''',
]
