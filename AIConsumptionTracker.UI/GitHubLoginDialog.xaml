<Window x:Class="AIConsumptionTracker.UI.GitHubLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login to GitHub" Height="300" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
        </Style>
    </Window.Resources>
    
    <Border Padding="20">
        <StackPanel VerticalAlignment="Center">
            
            <TextBlock Text="GitHub Authorization" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBlock Text="To verify your account, please copy the code below and enter it at:" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center"/>
            <TextBlock x:Name="VerificationUrlText" Text="https://github.com/login/device" Foreground="#58A6FF" Cursor="Hand" TextDecorations="Underline" HorizontalAlignment="Center" Margin="0,5,0,0" MouseDown="Url_MouseDown"/>

            <Border Background="#2D2D2D" CornerRadius="4" Padding="15" Margin="0,20,0,20" HorizontalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="UserCodeText" Text="----" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="Consolas, Monospace"/>
                    <Button x:Name="CopyBtn" Content="Copy" Margin="15,0,0,0" Padding="10,2" Click="CopyBtn_Click" Background="#3D3D3D" Foreground="White" BorderBrush="#555"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Width="20" Height="20" Margin="0,0,10,0" Visibility="Collapsed"/>
                <TextBlock x:Name="StatusText" Text="Initializing..." FontWeight="SemiBold"/>
            </StackPanel>

            <Button x:Name="CancelBtn" Content="Cancel" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20,0,0" Width="100" Padding="5" Background="Transparent" Foreground="#AAA" BorderThickness="0" Click="CancelBtn_Click"/>

        </StackPanel>
    </Border>
</Window>
